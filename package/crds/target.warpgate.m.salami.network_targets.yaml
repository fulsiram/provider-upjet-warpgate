---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: targets.target.warpgate.m.salami.network
spec:
  group: target.warpgate.m.salami.network
  names:
    categories:
    - crossplane
    - managed
    - warpgate
    kind: Target
    listKind: TargetList
    plural: targets
    singular: target
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Target is the Schema for the Targets API. Manages a target in
          Warpgate. A target represents a destination system that users can connect
          to.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TargetSpec defines the desired state of Target
            properties:
              forProvider:
                properties:
                  description:
                    description: |-
                      A human-readable description of the target.
                      The description of the target
                    type: string
                  groupId:
                    description: |-
                      The ID of the target group this target is assigned to.
                      Which target group this target is assigned to
                    type: string
                  groupIdRef:
                    description: Reference to a Group in target to populate groupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupIdSelector:
                    description: Selector for a Group in target to populate groupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  httpOptions:
                    description: |-
                      HTTP target configuration block.
                      HTTP target options
                    items:
                      properties:
                        externalHost:
                          description: |-
                            External host for HTTP requests.
                            External host for HTTP requests
                          type: string
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            HTTP headers to include in requests.
                            HTTP headers to include in requests
                          type: object
                          x-kubernetes-map-type: granular
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        url:
                          description: |-
                            The HTTP server URL.
                            The HTTP server URL
                          type: string
                      type: object
                    type: array
                  kubernetesOptions:
                    description: |-
                      (Block List, Max: 1) Kubernetes target options (see below for nested schema)
                      Kubernetes target options
                    items:
                      properties:
                        certificateAuth:
                          description: |-
                            (Block List, Max: 1) Certificate authentication for Kubernetes (see below for nested schema)
                            Certificate authentication for Kubernetes
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) The client certificate PEM
                                  The client certificate PEM
                                type: string
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) The client private key PEM
                                  The client private key PEM
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        clusterUrl:
                          description: |-
                            (String) The Kubernetes cluster URL
                            The Kubernetes cluster URL
                          type: string
                        tls:
                          description: |-
                            (Block List, Min: 1, Max: 1) TLS configuration (see below for nested schema)
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        tokenAuth:
                          description: |-
                            (Block List, Max: 1) Token authentication for Kubernetes (see below for nested schema)
                            Token authentication for Kubernetes
                          items:
                            properties:
                              tokenSecretRef:
                                description: |-
                                  (String, Sensitive) The bearer token for Kubernetes authentication
                                  The bearer token for Kubernetes authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                      type: object
                    type: array
                  mysqlOptions:
                    description: |-
                      MySQL target configuration block.
                      MySQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The MySQL server hostname or IP address.
                            The MySQL server hostname or IP address
                          type: string
                        passwordSecretRef:
                          description: |-
                            The MySQL password.
                            The MySQL password
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        port:
                          description: |-
                            The MySQL server port.
                            The MySQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The MySQL username.
                            The MySQL username
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      The name of the target. Must be unique within the Warpgate instance.
                      The name of the target
                    type: string
                  postgresOptions:
                    description: |-
                      PostgreSQL target configuration block.
                      PostgreSQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The PostgreSQL server hostname or IP address.
                            The PostgreSQL server hostname or IP address
                          type: string
                        passwordSecretRef:
                          description: |-
                            The PostgreSQL password.
                            The PostgreSQL password
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        port:
                          description: |-
                            The PostgreSQL server port.
                            The PostgreSQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The PostgreSQL username.
                            The PostgreSQL username
                          type: string
                      type: object
                    type: array
                  sshOptions:
                    description: |-
                      SSH target configuration block.
                      SSH target options
                    items:
                      properties:
                        allowInsecureAlgos:
                          description: |-
                            Allow insecure SSH algorithms. Default: false.
                            Allow insecure SSH algorithms
                          type: boolean
                        host:
                          description: |-
                            The SSH server hostname or IP address.
                            The SSH server hostname or IP address
                          type: string
                        passwordAuth:
                          description: |-
                            Password authentication for SSH. Conflicts with public_key_auth.
                            Password authentication for SSH
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The PostgreSQL password.
                                  The password for SSH authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        port:
                          description: |-
                            The SSH server port.
                            The SSH server port
                          type: number
                        publicKeyAuth:
                          description: |-
                            Public key authentication for SSH. Conflicts with password_auth. No additional properties needed.
                            Public key authentication for SSH
                          items:
                            type: object
                          type: array
                        username:
                          description: |-
                            The SSH username.
                            The SSH username
                          type: string
                      type: object
                    type: array
                type: object
              initProvider:
                description: |-
                  THIS IS A BETA FIELD. It will be honored
                  unless the Management Policies feature flag is disabled.
                  InitProvider holds the same fields as ForProvider, with the exception
                  of Identifier and other resource reference fields. The fields that are
                  in InitProvider are merged into ForProvider when the resource is created.
                  The same fields are also added to the terraform ignore_changes hook, to
                  avoid updating them after creation. This is useful for fields that are
                  required on creation, but we do not desire to update them after creation,
                  for example because of an external controller is managing them, like an
                  autoscaler.
                properties:
                  description:
                    description: |-
                      A human-readable description of the target.
                      The description of the target
                    type: string
                  groupId:
                    description: |-
                      The ID of the target group this target is assigned to.
                      Which target group this target is assigned to
                    type: string
                  groupIdRef:
                    description: Reference to a Group in target to populate groupId.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      namespace:
                        description: Namespace of the referenced object
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  groupIdSelector:
                    description: Selector for a Group in target to populate groupId.
                    properties:
                      matchControllerRef:
                        description: |-
                          MatchControllerRef ensures an object with the same controller reference
                          as the selecting object is selected.
                        type: boolean
                      matchLabels:
                        additionalProperties:
                          type: string
                        description: MatchLabels ensures an object with matching labels
                          is selected.
                        type: object
                      namespace:
                        description: Namespace for the selector
                        type: string
                      policy:
                        description: Policies for selection.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    type: object
                  httpOptions:
                    description: |-
                      HTTP target configuration block.
                      HTTP target options
                    items:
                      properties:
                        externalHost:
                          description: |-
                            External host for HTTP requests.
                            External host for HTTP requests
                          type: string
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            HTTP headers to include in requests.
                            HTTP headers to include in requests
                          type: object
                          x-kubernetes-map-type: granular
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        url:
                          description: |-
                            The HTTP server URL.
                            The HTTP server URL
                          type: string
                      type: object
                    type: array
                  kubernetesOptions:
                    description: |-
                      (Block List, Max: 1) Kubernetes target options (see below for nested schema)
                      Kubernetes target options
                    items:
                      properties:
                        certificateAuth:
                          description: |-
                            (Block List, Max: 1) Certificate authentication for Kubernetes (see below for nested schema)
                            Certificate authentication for Kubernetes
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) The client certificate PEM
                                  The client certificate PEM
                                type: string
                              privateKeySecretRef:
                                description: |-
                                  (String, Sensitive) The client private key PEM
                                  The client private key PEM
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - privateKeySecretRef
                            type: object
                          type: array
                        clusterUrl:
                          description: |-
                            (String) The Kubernetes cluster URL
                            The Kubernetes cluster URL
                          type: string
                        tls:
                          description: |-
                            (Block List, Min: 1, Max: 1) TLS configuration (see below for nested schema)
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        tokenAuth:
                          description: |-
                            (Block List, Max: 1) Token authentication for Kubernetes (see below for nested schema)
                            Token authentication for Kubernetes
                          items:
                            properties:
                              tokenSecretRef:
                                description: |-
                                  (String, Sensitive) The bearer token for Kubernetes authentication
                                  The bearer token for Kubernetes authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - tokenSecretRef
                            type: object
                          type: array
                      type: object
                    type: array
                  mysqlOptions:
                    description: |-
                      MySQL target configuration block.
                      MySQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The MySQL server hostname or IP address.
                            The MySQL server hostname or IP address
                          type: string
                        passwordSecretRef:
                          description: |-
                            The MySQL password.
                            The MySQL password
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        port:
                          description: |-
                            The MySQL server port.
                            The MySQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The MySQL username.
                            The MySQL username
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      The name of the target. Must be unique within the Warpgate instance.
                      The name of the target
                    type: string
                  postgresOptions:
                    description: |-
                      PostgreSQL target configuration block.
                      PostgreSQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The PostgreSQL server hostname or IP address.
                            The PostgreSQL server hostname or IP address
                          type: string
                        passwordSecretRef:
                          description: |-
                            The PostgreSQL password.
                            The PostgreSQL password
                          properties:
                            key:
                              type: string
                            name:
                              description: Name of the secret.
                              type: string
                          required:
                          - key
                          - name
                          type: object
                        port:
                          description: |-
                            The PostgreSQL server port.
                            The PostgreSQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The PostgreSQL username.
                            The PostgreSQL username
                          type: string
                      type: object
                    type: array
                  sshOptions:
                    description: |-
                      SSH target configuration block.
                      SSH target options
                    items:
                      properties:
                        allowInsecureAlgos:
                          description: |-
                            Allow insecure SSH algorithms. Default: false.
                            Allow insecure SSH algorithms
                          type: boolean
                        host:
                          description: |-
                            The SSH server hostname or IP address.
                            The SSH server hostname or IP address
                          type: string
                        passwordAuth:
                          description: |-
                            Password authentication for SSH. Conflicts with public_key_auth.
                            Password authentication for SSH
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The PostgreSQL password.
                                  The password for SSH authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            required:
                            - passwordSecretRef
                            type: object
                          type: array
                        port:
                          description: |-
                            The SSH server port.
                            The SSH server port
                          type: number
                        publicKeyAuth:
                          description: |-
                            Public key authentication for SSH. Conflicts with password_auth. No additional properties needed.
                            Public key authentication for SSH
                          items:
                            type: object
                          type: array
                        username:
                          description: |-
                            The SSH username.
                            The SSH username
                          type: string
                      type: object
                    type: array
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
            x-kubernetes-validations:
            - message: spec.forProvider.name is a required parameter
              rule: '!(''*'' in self.managementPolicies || ''Create'' in self.managementPolicies
                || ''Update'' in self.managementPolicies) || has(self.forProvider.name)
                || (has(self.initProvider) && has(self.initProvider.name))'
          status:
            description: TargetStatus defines the observed state of Target.
            properties:
              atProvider:
                properties:
                  allowRoles:
                    description: |-
                      The list of roles allowed to access this target (computed from role assignments).
                      The list of roles allowed to access this target
                    items:
                      type: string
                    type: array
                  description:
                    description: |-
                      A human-readable description of the target.
                      The description of the target
                    type: string
                  groupId:
                    description: |-
                      The ID of the target group this target is assigned to.
                      Which target group this target is assigned to
                    type: string
                  httpOptions:
                    description: |-
                      HTTP target configuration block.
                      HTTP target options
                    items:
                      properties:
                        externalHost:
                          description: |-
                            External host for HTTP requests.
                            External host for HTTP requests
                          type: string
                        headers:
                          additionalProperties:
                            type: string
                          description: |-
                            HTTP headers to include in requests.
                            HTTP headers to include in requests
                          type: object
                          x-kubernetes-map-type: granular
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        url:
                          description: |-
                            The HTTP server URL.
                            The HTTP server URL
                          type: string
                      type: object
                    type: array
                  id:
                    description: The ID of the target.
                    type: string
                  kubernetesOptions:
                    description: |-
                      (Block List, Max: 1) Kubernetes target options (see below for nested schema)
                      Kubernetes target options
                    items:
                      properties:
                        certificateAuth:
                          description: |-
                            (Block List, Max: 1) Certificate authentication for Kubernetes (see below for nested schema)
                            Certificate authentication for Kubernetes
                          items:
                            properties:
                              certificate:
                                description: |-
                                  (String) The client certificate PEM
                                  The client certificate PEM
                                type: string
                            type: object
                          type: array
                        clusterUrl:
                          description: |-
                            (String) The Kubernetes cluster URL
                            The Kubernetes cluster URL
                          type: string
                        tls:
                          description: |-
                            (Block List, Min: 1, Max: 1) TLS configuration (see below for nested schema)
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        tokenAuth:
                          description: |-
                            (Block List, Max: 1) Token authentication for Kubernetes (see below for nested schema)
                            Token authentication for Kubernetes
                          items:
                            properties:
                              tokenSecretRef:
                                description: |-
                                  (String, Sensitive) The bearer token for Kubernetes authentication
                                  The bearer token for Kubernetes authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                      type: object
                    type: array
                  mysqlOptions:
                    description: |-
                      MySQL target configuration block.
                      MySQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The MySQL server hostname or IP address.
                            The MySQL server hostname or IP address
                          type: string
                        port:
                          description: |-
                            The MySQL server port.
                            The MySQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The MySQL username.
                            The MySQL username
                          type: string
                      type: object
                    type: array
                  name:
                    description: |-
                      The name of the target. Must be unique within the Warpgate instance.
                      The name of the target
                    type: string
                  postgresOptions:
                    description: |-
                      PostgreSQL target configuration block.
                      PostgreSQL target options
                    items:
                      properties:
                        host:
                          description: |-
                            The PostgreSQL server hostname or IP address.
                            The PostgreSQL server hostname or IP address
                          type: string
                        port:
                          description: |-
                            The PostgreSQL server port.
                            The PostgreSQL server port
                          type: number
                        tls:
                          description: |-
                            TLS configuration block.
                            TLS configuration
                          items:
                            properties:
                              mode:
                                description: |-
                                  TLS mode. Valid values: Disabled, Preferred, Required.
                                  TLS mode (Disabled, Preferred, Required)
                                type: string
                              verify:
                                description: |-
                                  Verify TLS certificates.
                                  Verify TLS certificates
                                type: boolean
                            type: object
                          type: array
                        username:
                          description: |-
                            The PostgreSQL username.
                            The PostgreSQL username
                          type: string
                      type: object
                    type: array
                  sshOptions:
                    description: |-
                      SSH target configuration block.
                      SSH target options
                    items:
                      properties:
                        allowInsecureAlgos:
                          description: |-
                            Allow insecure SSH algorithms. Default: false.
                            Allow insecure SSH algorithms
                          type: boolean
                        host:
                          description: |-
                            The SSH server hostname or IP address.
                            The SSH server hostname or IP address
                          type: string
                        passwordAuth:
                          description: |-
                            Password authentication for SSH. Conflicts with public_key_auth.
                            Password authentication for SSH
                          items:
                            properties:
                              passwordSecretRef:
                                description: |-
                                  The PostgreSQL password.
                                  The password for SSH authentication
                                properties:
                                  key:
                                    type: string
                                  name:
                                    description: Name of the secret.
                                    type: string
                                required:
                                - key
                                - name
                                type: object
                            type: object
                          type: array
                        port:
                          description: |-
                            The SSH server port.
                            The SSH server port
                          type: number
                        publicKeyAuth:
                          description: |-
                            Public key authentication for SSH. Conflicts with password_auth. No additional properties needed.
                            Public key authentication for SSH
                          items:
                            type: object
                          type: array
                        username:
                          description: |-
                            The SSH username.
                            The SSH username
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
